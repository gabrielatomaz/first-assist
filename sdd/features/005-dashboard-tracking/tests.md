# Test Scope — Dashboard & Tracking (FEAT-005)

## Test Cases

### 1. API Verification
* **Test Case**: `PATCH /api/incidents/:id/status` returns 200 and maps changes.
* **Test Case**: Fetching with filter queries restricts returned objects correctly.

### 2. View Verifications
* **Test Case**: Selecting status parameters filters cards from display lists.
